package com.makeup.gateway.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class GatewayConfig {
    // Boilerplate for custom filters, CORS, rate-limiting or security headers
}
